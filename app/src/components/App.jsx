import React from 'react';
import {Route} from 'react-router-dom';

const App = () => (
  <div>
    <button className="btn btn-primary">Test</button>
    <button className="btn btn-primary">Test Buddy</button>
    <div>React autocomplete still does not work</div>
  </div>
)

export default App;
